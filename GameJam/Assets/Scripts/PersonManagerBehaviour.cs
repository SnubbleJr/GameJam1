using UnityEngine;
using System.Collections;

public class PersonManagerBehaviour : MonoBehaviour {

	// Use this for initialization
	void Start () {
		BroadcastMessage("spawnPerson");
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
